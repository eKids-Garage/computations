[tool.poetry]
name = "repl_python3_computations-22"
version = "0.1.0"
description = ""
authors = ["ksushaveko <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
pytest = "^6.2.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
